spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/linelearn_security
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.example.com
    port: 587
    username: anas.joo@esprit.tn
    password: ISMAILproba000@+++
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 5001
